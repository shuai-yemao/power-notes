{
  "name": "power-notes-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local MCP destination server for importing normalized notes into Power Notes.",
  "scripts": {
    "start": "node src/server.mjs",
    "smoke": "node test/smoke.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0"
  }
}

# Platform 与 Impl：一条不被硬件绑架的依赖方向

可移植性不是把所有代码都写成宏，而是让上层只依赖稳定的能力契约。

## 一条清晰的依赖方向

推荐的关系是：

```text
App → Service → Platform 接口 ← Impl/BSP → Vendor/HAL
```

Platform 只定义能力、类型、错误码和对象协议；具体芯片、板卡和 RTOS 资源在 Impl 中绑定。

## 边界的价值

当硬件替换时，变化应该停留在实现侧。上层测试可以继续验证业务逻辑，目标板验证则专注于真实外设和时序证据。
